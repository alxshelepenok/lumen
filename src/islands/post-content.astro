---
import styles from "@/assets/styles/modules/post-content.module.scss";

interface Props {
  body: string;
  title: string;
}

const { body, title } = Astro.props;
---

<div class={styles.postContent}>
  <h1 class={styles.title}>{title}</h1>
  <div class={styles.body} set:html={body} />
</div>
