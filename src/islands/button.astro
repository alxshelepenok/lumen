---
import styles from "@/assets/styles/modules/button.module.scss";

interface Props {
  className?: string;
  title: string;
  to: string;
}

const { className, title, to } = Astro.props;
---

<a class={[styles.button, className].filter(Boolean).join(" ")} href={to}>
  {title}
</a>
